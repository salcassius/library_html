name: Deploy HTML Site to GitHub Pages

on:
  push:
    branches:
      - main  # ឬប្តូរទៅ master ប្រសិនបើ branch របស់អ្នកឈ្មោះ master

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: . # ចម្លង File ទាំងអស់ចេញពី Root folder
          branch: gh-pages
