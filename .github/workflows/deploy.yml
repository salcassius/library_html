name: Deploy HTML to GitHub Pages and FTP

on:
  push:
    branches:
      - main 

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      # --- ១. ផ្នែក Deploy ទៅ GitHub Pages ---
      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: . 
          branch: gh-pages

      # --- ២. ផ្នែក Deploy ទៅ FTP Server (Hosting) ---
      - name: FTP Deploy 🚀
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: /public_html/ # ប្តូរទៅកាន់ Folder លើ Server របស់អ្នក