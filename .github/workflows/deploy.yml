name: Deploy Library System to GitHub Pages

on:
  push:
    branches:
      - main  # វានឹងដំណើរការរាល់ពេលអ្នក push កូដទៅកាន់ branch main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies 🔧
        run: npm install

      - name: Build Project 🏗️
        run: npm run build

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Folder ដែល Vue build រួច (ធម្មតាគឺ dist)
          branch: gh-pages # Branch ដែលទុកសម្រាប់បង្ហាញ website
